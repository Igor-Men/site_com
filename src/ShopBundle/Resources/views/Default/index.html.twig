{% extends '@Shop/base.html.twig' %}

{% block body %}

    <div class="container">

        <div class="row row-offcanvas row-offcanvas-right">

            <div class="col-xs-12 col-sm-9">
                <p class="pull-right visible-xs">
                    <button type="button" class="btn btn-primary btn-xs" data-toggle="offcanvas">Toggle nav</button>
                </p>
                <div class="jumbotron">
                    <h1>Hello, world!</h1>
                    <p>Справа категории, внизу пагинатор, через минуту вылезет попап.</p>
                    <p>стандартный юзер login:admin pass:admin</p>
                    <p>для примера видно 4ри товара и три категории, остальное "рыба" без связей</p>
                </div>
                <div class="row">

                    {% for product in pagination %}
                        <div class="col-xs-6 col-lg-4">
                            <h2>{{ product.name }}</h2>
                            <span>{{ product.price }}</span>
                            <p>some description am porta sem malesuada magna mollis euismod. Donec sed odio dui. </p>
                            <p><a class="btn btn-default" href="{{ path('product_name', {
                                    'name' : product.name
                                }) }}" role="button">View details &raquo;</a></p>
                        </div><!--/.col-xs-6.col-lg-4-->
                    {% endfor %}

                </div><!--/row-->
            </div><!--/.col-xs-12.col-sm-9-->

            <div class="col-xs-6 col-sm-3 sidebar-offcanvas" id="sidebar">
                <div class="list-group">

                    {%  for category in categories %}
                        <a href="{{ path('category_name', {
                            'name' : category.name
                        }) }}" class="list-group-item">{{ category.name }} ({{ category.products|length }})</a>
                    {% endfor %}
                </div>
            </div><!--/.sidebar-offcanvas-->
        </div><!--/row-->

        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>
        <hr>

        <footer>
            <p>&copy; 2016 site_com.</p>
        </footer>

    </div><!--/.container-->

{% endblock %}
